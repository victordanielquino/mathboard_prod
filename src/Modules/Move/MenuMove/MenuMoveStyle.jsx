import {makeStyles} from "@mui/styles";

const useStylesMenuMove  = makeStyles({
    container: {
        borderRadius: '10px',
        borderBottom: '1px solid var(--very-light-pink)',
        backgroundColor: 'white',
        display: 'flex',
        padding: '5px 20px',
        justifyContent: 'space-around',
        alignItems: 'center',
        //width: '170px',
        color: 'black',
        marginLeft: '1em',
    }
});

export default useStylesMenuMove;