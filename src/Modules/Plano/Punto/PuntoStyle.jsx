import {makeStyles} from "@mui/styles";

const useStylesPunto  = makeStyles({
    containedPto: {
        display: 'flex',
        alignItems:'center',
        justifyContent:'flex-start'
    }
});

export default useStylesPunto;