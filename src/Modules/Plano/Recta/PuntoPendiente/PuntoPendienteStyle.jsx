import {makeStyles} from "@mui/styles";

const useStylesPuntoPendiente = makeStyles({
    containedPto: {
        display: 'flex',
        alignItems:'center',
        justifyContent:'flex-start'
    },
    divFraction: {
        display:'flex',
        flexDirection:'column',
        alignItems:'center',
        justifyContent:'center',
    }
});

export default useStylesPuntoPendiente;