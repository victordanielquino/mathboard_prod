import {makeStyles} from "@mui/styles";

const useStylesRectaPasaPorDosPuntos  = makeStyles({
    containedPto: {
        display: 'flex',
        alignItems:'center',
        justifyContent:'flex-start'
    }
});

export default useStylesRectaPasaPorDosPuntos;