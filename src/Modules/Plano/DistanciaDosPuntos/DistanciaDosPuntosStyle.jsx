import {makeStyles} from "@mui/styles";

const useStylesDistanciaDosPuntos  = makeStyles({
    containedPto: {
        display: 'flex',
        alignItems:'center',
        justifyContent:'flex-start'
    }
});

export default useStylesDistanciaDosPuntos;